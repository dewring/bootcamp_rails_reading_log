<div class="form-card">
  <div class="form-header">
    <h1>Edit Book</h1>
    <p><%= @book.title %></p>
  </div>

  <%= render "form", book: @book %>

  <div class="form-cancel">
    <%= link_to "← Cancel", @book %>
  </div>
</div>
